var StateProvider =  require('./dist/state.provider').StateProvider;

console.log('todos', StateProvider);

var newState = StateProvider.createState('todoApp', {
  todos: []
})

console.log('newState', newState.get());
